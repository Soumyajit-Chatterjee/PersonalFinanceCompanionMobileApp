package com.example.personalfinancecompanionmobileapp.ui.theme

import androidx.compose.ui.graphics.Color

// Earthy Palette based on the requested design
val EarthOlive = Color(0xFF606C38)
val EarthForest = Color(0xFF283618)
val EarthCream = Color(0xFFFEFAE0)
val EarthGold = Color(0xFFDDA15E)
val EarthTerra = Color(0xFFBC6C25)

// Functional Colors
val IncomeGreen = EarthOlive
val ExpenseRed = EarthTerra

// Glassmorphism Utilities
val GlassSurfaceWhite = Color.White.copy(alpha = 0.5f)
val GlassBorderWhite = Color.White.copy(alpha = 0.8f)

val GlassSurfaceDark = Color.White.copy(alpha = 0.10f)
val GlassBorderDark = Color.White.copy(alpha = 0.25f)